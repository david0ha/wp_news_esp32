# Never print these

One per line, with a reason if you want to remember it later.

This is the same store as a directive posted to `POST /api/directives` with a
rule that must hold forever, kept as its own file because "what must never
print" is the one list worth being able to read at a glance — and because a
file you can open beats an endpoint you have to query when you are trying to
work out why something appeared.

- (nothing yet)
