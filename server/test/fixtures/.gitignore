# The fixtures are generated by test_proofpng.py at run time, never committed.
# A binary blob in a public repository is a thing nobody can review in a diff,
# and the code that writes these is the documentation of the format being
# parsed -- which a checked-in file would not be.
*.bmp
*.png
