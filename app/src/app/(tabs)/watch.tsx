import { TabPlaceholder } from '../../components/TabPlaceholder'

// Watch — the watchlist, each row led by its grade and its thesis line, the quote subordinate
// (plan Design > Wireframes). Task 27 builds it.
export default function Watch() {
  return (
    <TabPlaceholder
      title="Watch"
      note="The companies the desk is watching, each with the grade and the argument behind it."
    />
  )
}
