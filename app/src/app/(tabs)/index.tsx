import { TabPlaceholder } from '../../components/TabPlaceholder'

// Today — the masthead, the sheet on the glass, and the day's edition set in one column
// (plan Design > Wireframes). Task 24 builds it; this is the route and the gear.
export default function Today() {
  return (
    <TabPlaceholder
      title="Today"
      note="The day's edition lands here — the sheet on the glass, the lead, and the dossier."
    />
  )
}
