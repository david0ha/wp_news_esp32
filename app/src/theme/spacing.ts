// Spacing scale — plan Design > "Space, radius, motion". Named by size rather than by intent
// (xs/sm/lg…) because the redesign's own wireframes mix the same 16 in a card's inner margin and
// a screen's gutter; a semantic name would have had to pick one.
export const spacing = {
  4: 4,
  8: 8,
  12: 12,
  16: 16,
  24: 24,
  32: 32,
  40: 40,
} as const
